{
  "name": "pdf-print-marks",
  "version": "1.0.4",
  "main": "dist/index",
  "license": "MIT",
  "scripts": {
    "start": "ts-node index.ts",
    "test": "ts-node test/generateTestFiles.ts",
    "build": "rm -rf dist && tsc",
    "prepare": "yarn build"
  },
  "dependencies": {
    "@cantoo/pdf-lib": "^1.21.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "ts-node": "^10.9.2",
    "tslib": "^2.6.2",
    "typescript": "^5.4.5"
  },
  "files": [
    "dist",
    "src",
    "index.ts"
  ]
}
